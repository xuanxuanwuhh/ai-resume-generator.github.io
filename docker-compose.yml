services:
  jekyll:
    image: jekyll/jekyll:4.2.2
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll
    command: jekyll serve --watch --force_polling --host 0.0.0.0 --port 4000
